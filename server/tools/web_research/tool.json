{
  "name": "Web Research",
  "description": "Search the web AND read the top result pages in one step, returning their actual content (facts, numbers, prices, news) — not just links. Use for anything current or external.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "What to research, e.g. 'weather in Genova today'"
      }
    },
    "required": [
      "query"
    ]
  },
  "timeout": 150,
  "max_output": 8000
}

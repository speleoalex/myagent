{
  "name": "Manage Tasks",
  "description": "Your own scheduled tasks: what you will do unattended, and when. 'list' answers \"what is my next task?\", 'add' schedules new work, 'update' changes one, 'delete' cancels one. Tasks only run while your autonomous mode is on.",
  "internal": true,
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["list", "add", "update", "delete"],
        "description": "What to do with your tasks."
      },
      "prompt": {
        "type": "string",
        "description": "What to do when the task runs, written so your future self understands it without any other context. Required for 'add'."
      },
      "cron": {
        "type": "string",
        "description": "Recurring: 5 fields 'minute hour day month weekday' (0=Sunday). '*/20 * * * *' every 20 minutes, '0 9 * * 1' Mondays at 09:00, '30 7 * * 1-5' weekdays at 07:30. Never build one out of a delay: 'in 10 minutes' is in_s=600, NOT '0 10 * * *'."
      },
      "in_s": {
        "type": "integer",
        "description": "One-off, this many seconds from now ('in 10 minutes' = 600). Use this for a DELAY, never for a time of day that repeats."
      },
      "at": {
        "type": "string",
        "description": "One-off at an ISO timestamp, e.g. '2026-07-30T18:15'. Only for a wall-clock time the user named: you do not reliably know the current time."
      },
      "task_id": {
        "type": "string",
        "description": "Which task to update or delete, as shown by 'list'."
      }
    },
    "required": [
      "action"
    ]
  }
}

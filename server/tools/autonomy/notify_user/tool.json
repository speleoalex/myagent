{
  "name": "Notify User",
  "description": "Send a message to somebody in the address book: a person on their messaging channel (Telegram, email), or a device that reads it aloud. Use it to tell someone something proactively: when you run unattended your reply text reaches nobody, this does. The subject goes in 'subject' and the files in 'attachments', never inside 'text'.",
  "internal": true,
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The message to send"
      },
      "to": {
        "type": "string",
        "description": "Who gets it: a name from the address book, person or device. Pick one of the listed values when there are any, otherwise write the name and the error will list who exists. \"all\" sends to everyone on that channel, only when you were asked to tell everyone. Omit to use the agent's configured target. Never invent a numeric id."
      },
      "channel": {
        "type": "string",
        "description": "Which messaging channel, only when more than one could apply."
      },
      "subject": {
        "type": "string",
        "description": "Optional. The Subject on email, the first line on a chat."
      },
      "attachments": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Optional files to send along, by the name they have in the workspace. Max 5, 15 MB each."
      }
    },
    "required": [
      "text"
    ]
  }
}

{
  "name": "Make Directory",
  "description": "Create a directory and any missing parents. Succeeds if it already exists.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Directory path to create"
      }
    },
    "required": [
      "path"
    ]
  },
  "timeout": 10,
  "max_output": 500
}

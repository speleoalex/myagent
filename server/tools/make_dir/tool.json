{
  "name": "Make Directory",
  "description": "Create a directory, including any missing parent directories. Succeeds even if the directory already exists.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Directory path to create"
      }
    },
    "required": ["path"]
  },
  "timeout": 10,
  "max_output": 500
}

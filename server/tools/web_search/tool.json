{
  "name": "Web Search",
  "description": "Search the web using DuckDuckGo. Returns a list of results with title, URL and snippet. Use this to find information online.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query (e.g. 'best caves in Italy')"
      }
    },
    "required": ["query"]
  },
  "timeout": 45,
  "max_output": 10000
}

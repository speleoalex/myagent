{
  "name": "Activate Tools",
  "description": "Load the tools of one category so you can call them. You start a turn with the list of categories you may switch on, but without the tools themselves: call this first with the category you need, then call the tools it returns. Activations last for the rest of this turn, and asking for one costs you nothing.",
  "internal": true,
  "parameters": {
    "type": "object",
    "properties": {
      "category": {
        "type": "string",
        "description": "The category to switch on."
      }
    },
    "required": ["category"]
  }
}

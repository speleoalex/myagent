{
  "name": "Memory Search",
  "description": "Search your long-term memory (saved notes and summaries of past conversations) by keywords. Returns matching memory ids with a short summary - explicit notes rank first; use memory_read on an id for the details. Only works for agents with memory enabled.",
  "internal": true,
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Keywords to look for (e.g. 'deploy telegram backup')"
      }
    },
    "required": [
      "query"
    ]
  }
}

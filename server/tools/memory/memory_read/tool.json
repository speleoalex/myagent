{
  "name": "Memory Read",
  "description": "Read one item of your long-term memory by id (from memory_search or the Memory index), e.g. '260730143012'. A note returns its full text; a conversation memory returns the archived summary and names the session holding the full transcript. Only works for agents with memory enabled.",
  "internal": true,
  "parameters": {
    "type": "object",
    "properties": {
      "node_id": {
        "type": "string",
        "description": "The memory id, e.g. '260730143012'"
      }
    },
    "required": [
      "node_id"
    ]
  }
}

{
  "name": "Append to File",
  "description": "Append content to the end of a file, creating it and any parent directories. Use instead of file_write to add without overwriting.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "File path to append to"
      },
      "content": {
        "type": "string",
        "description": "Content to append"
      }
    },
    "required": [
      "path",
      "content"
    ]
  },
  "timeout": 10,
  "max_output": 1000
}

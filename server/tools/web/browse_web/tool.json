{
  "name": "Browse Web",
  "description": "Fetch a webpage with a real (headless) browser and return its text; renders JavaScript. To search, browse https://duckduckgo.com/?q=your+terms (Google blocks automation).",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL to browse"
      },
      "extract": {
        "type": "string",
        "description": "'text' (visible text, default) or 'html'",
        "enum": [
          "text",
          "html"
        ],
        "default": "text"
      }
    },
    "required": [
      "url"
    ]
  },
  "timeout": 45,
  "max_output": 15000
}

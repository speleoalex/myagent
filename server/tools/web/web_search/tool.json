{
  "name": "Web Search",
  "description": "Search the web (DuckDuckGo) and return title, URL and snippet for each result.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query"
      }
    },
    "required": [
      "query"
    ]
  },
  "timeout": 45,
  "max_output": 10000
}

# Core runtime — deliberately minimal.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
httpx>=0.27.0

# Optional, per-tool (not required by the core app):
#   libzim>=3.4.0   # only for the `local_search` tool (offline ZIM knowledge base)

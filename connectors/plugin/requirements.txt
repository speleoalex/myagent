# Extra dependency of the connectors plugin, installed into myagent's own
# virtualenv by install.sh. Everything else the plugin needs (fastapi, pydantic,
# httpx) is already in server/requirements.txt — it runs in that process.
#
# Only used for Telegram voice notes, through the document_extract tool. It
# pulls in ctranslate2/tokenizers/onnxruntime (~380 MB) and wants ffmpeg on the
# PATH; skip it and the plugin still works, minus voice transcription.
faster-whisper>=1.0.0

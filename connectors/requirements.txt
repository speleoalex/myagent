# MyAgent Connectors — standalone messaging-bridge server.
# Deliberately minimal, same stack as myagent/server.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
httpx>=0.27.0
# Speech-to-text for voice notes (transcribed at the edge). Needs ffmpeg on the
# system PATH. The Whisper model is downloaded on first use and cached.
faster-whisper>=1.0.0
